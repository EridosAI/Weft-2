"""Phase 1 explorer placeholder.

Phase 1 uses cached embeddings (`data/dinov2_embeddings/embeddings.npy`)
and does not run an AI2-THOR controller at training time. No implementation
is required for v0; this stub exists only so the §2 directory layout is
consistent across phases. Phase 2/3 explorers wrap the prior repo's
`Weft/src/env/furniture_route_explorer.py` with perturbation logic.
"""
